import { NavHeaderItems } from './NavHeaderItems';
import React from 'react';

export const DesktopNavMenu = function () {

  return (
      <nav id="desktop-header-nav-menu">
        <NavHeaderItems></NavHeaderItems>
      </nav>
  );
}

export default DesktopNavMenu;
