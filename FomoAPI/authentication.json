{
  "Authentication": {
    "Google": {
      // Test credentials only allows access through local address. Replaced in production.
      "ClientId": "${CLIENT_ID}$",
      "ClientSecret": "${CLIENT_SECRET}$"
    }
  }
}